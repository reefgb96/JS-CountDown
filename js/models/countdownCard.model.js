"use strict";

class CountDownCard {
  title;
  logo;
  eventDate;
  id;
  constructor(title, logo, eventDate, id) {
    this.title = title;
    this.logo = logo;
    this.eventDate = eventDate;
    this.id = id;
  }
}
